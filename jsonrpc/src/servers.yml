langservers:
  pythonServer:
    - pyls
  python3Server:
    - pyls
  cppServer:
    - clangd
    - "--all-scopes-completion=true"
  cServer:
    - clangd
    - "--all-scopes-completion=true"
  ts:
    - typescript-language-server.cmd
    - --stdio

